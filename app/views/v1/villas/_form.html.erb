<%= form_with(model: v1_villa, local: true) do |form| %>
  <% if v1_villa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(v1_villa.errors.count, "error") %> prohibited this v1_villa from being saved:</h2>

      <ul>
      <% v1_villa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
